很抱歉当时设计的时候没有想的特别清晰让这里产生了这么多sh

其实最理想的方式应该是通过vllm/sglang拉起某个模型的服务，然后代码里通过http批量请求，但是由于我们的内部服务器禁止自行在端口上启动模型服务，我们只能采取离线serving的方式完成这些步骤。

其中，hope.sh/local.sh都是用于model_choice步骤的，尝试性生成一轮次（如果不需要选型可以跳过）
run*.sh则是用于实际场景生产的:)
phrase.sh是在最后结束后使用的，用于将对话改写的更拟人~

这里存在三种类型的